const checkButoon = document.querySelector(".checkBtn");
const inputField = document.querySelector(".userAnswer");
const massage = document.querySelector(".resultMassage");

checkButoon.addEventListener("click", () => {
  const userAnswer = Number(inputField.value);
});
